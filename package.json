{
  "name": "zakat-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bilingual Zakat calculator and payment tracker",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
